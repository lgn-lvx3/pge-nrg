{
	"name": "api",
	"version": "1.0.0",
	"description": "",
	"scripts": {
		"build": "tsc",
		"watch": "tsc -w",
		"clean": "rimraf dist",
		"prestart": "npm run clean && npm run build",
		"start": "yarn npm-run-all --parallel start watch",
		"test": "echo \"No tests yet...\"",
		"dev": "yarn npm-run-all --parallel start watch"
	},
	"dependencies": {},
	"devDependencies": {
		"@azure/functions": "^3.0.0",
		"@types/node": "^20.x",
		"npm-run-all": "^4.1.5",
		"rimraf": "^5.0.0",
		"typescript": "^4.0.0"
	}
}
